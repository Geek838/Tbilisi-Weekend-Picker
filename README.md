# Tbilisi Weekend Picker

Next.js app for deciding where to go in Tbilisi using Google Places API.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Create `.env.local`:
   ```bash
   GOOGLE_PLACES_API_KEY=your_key_here
   ```
3. Run:
   ```bash
   npm run dev
   ```
